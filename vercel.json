{
  "version": 2,
  "builds": [
    {
      "src": "src/main.js",
      "use": "@vercel/static-build",
      "config": {
        "maxDuration": 3600
      }
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    {
      "src": "/.*",
      "dest": "/index.html"
    }
  ]
}
